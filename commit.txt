Add Airtable integration via Vercel serverless function

- New api/submit.js proxies form submissions to Airtable
- Keeps API token server-side via environment variables
- Updated form JS to call /api/submit instead of Apps Script
