Add duplicate email check to signup flow

- api/submit.js now checks Airtable for existing email before insert
- Returns 409 with friendly message on duplicate
- Frontend distinguishes duplicate vs generic error states
